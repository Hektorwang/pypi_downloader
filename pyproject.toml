[project]
name = "pypi-downloader"
version = "0.8.1"
description = "Async PyPI package downloader with mirror fallback, automatic dependency resolution, and platform filtering"
requires-python = ">=3.11"
dependencies = [
    "aiohttp>=3.11",
    "loguru>=0.6",
    "rich>=12.0",
    "pip-tools>=7.0.0",
    "packaging>=21.0",
]
license = {text = "MIT"}
readme = "README.md"
authors = [
    { name = "Hektor", email = "hektorwang@gmail.com" }
]
keywords = ["pypi", "mirror", "download", "async", "package-manager"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Software Development :: Build Tools",
    "Topic :: System :: Archiving :: Mirroring",
]

[project.optional-dependencies]
full = [
    "pypiserver>=2.0.0",  # For --serve (offline private PyPI server)
]

[project.scripts]
pypi-downloader = "pypi_downloader.cli:main"
    
[build-system]
requires = ["uv_build>=0.8.3,<0.9.0"]
build-backend = "uv_build"
